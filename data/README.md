# Data

This project expects a file at:

- `data/census.csv`

To run the notebook, copy `census.csv` into this folder.
(For example, you can copy it from the [archived Udacity repo](https://github.com/andigles/udacity-finding-donors-archive))
